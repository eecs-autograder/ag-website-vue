import * as ag_cli from 'ag-client-typescript';
import * as sinon from 'sinon';

import * as data_ut from '@/tests/data_utils';
import { managed_mount } from '@/tests/setup';

test('Load file on open', async () => {
    fail();
});
